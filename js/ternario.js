const cuenta = 10;
//let mensaje;

const mensaje =
    cuenta < 0 ? "Cuenta Negativa" : "cuenta Positiva";


// if (cuenta < 0)
//    mensaje = "No saldo"
//else
//    mensaje = "Saldo Positivo"

document.write(mensaje);
